import type { Metadata } from 'next'
import { DocsCard, DocsShell } from '@/components/docs-shell'
import { docsGroups, docSections } from '@/data/docs'

export const metadata: Metadata = { title: 'Documentation — Natural Intellects', description: 'How Natural Intellects builds, what it has built, what it explores and why the Foundation exists.' }

export default function DocsPage() { return <DocsShell><div className="max-w-4xl"><div className="font-mono text-[10px] uppercase tracking-[.22em] text-accent">Natural Intellects / technical knowledge base</div><h1 className="mt-6 max-w-4xl text-6xl leading-[.9] tracking-[-.08em] sm:text-8xl">How we build.<br /><span className="text-accent">What we explore.</span></h1><p className="mt-8 max-w-2xl text-xl leading-8 text-muted">A public record of what Natural Intellects is, how its systems are engineered, what has been built, what remains exploratory and how the Foundation fits into the organization.</p></div><div className="mt-16 grid gap-4 sm:grid-cols-2">{docsGroups.map((group) => { const first = docSections.find((doc) => doc.group === group); return <DocsCard key={group} href={first ? `/docs/${first.slug}` : '/docs'} label={group} summary={first?.summary || 'Documentation area'} count={`${String(docsGroups.indexOf(group) + 1).padStart(2, '0')} / area`} /> })}</div><div className="mt-16 border-t border-line pt-7"><div className="font-mono text-[10px] uppercase tracking-[.18em] text-accent">Reading the documentation</div><p className="mt-4 max-w-2xl text-sm leading-7 text-muted">Implementation status is evidence-led. Concepts are labeled as concepts. Planned work is not described as shipped. Where a technical detail is not verified, it is omitted or marked for verification.</p></div></DocsShell> }
