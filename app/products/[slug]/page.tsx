import Link from 'next/link'
import { ArrowLeft, ArrowUpRight } from 'lucide-react'
import { allSlugs, findProject, projectName } from '@/data/site'
import { SiteNav, Status } from '@/components/site-chrome'
import { notFound } from 'next/navigation'

export function generateStaticParams() { return allSlugs().products.map((slug) => ({ slug })) }
export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const p = findProject(slug); if (!p) notFound(); return <main><SiteNav /><article className="mx-auto max-w-7xl px-6 pb-24 pt-40 lg:px-10 lg:pt-52"><Link href="/#products" className="font-mono text-xs uppercase tracking-widest text-muted hover:text-accent"><ArrowLeft className="mr-2 inline size-3" /> Back to products</Link><div className="mt-16 grid gap-14 lg:grid-cols-[.8fr_1.2fr]"><div><div className="eyebrow">Product / {p.category}</div><h1 className="mt-7 text-6xl tracking-[-.07em] lg:text-9xl">{p.name}</h1><div className="mt-8"><Status value={p.status} /></div></div><div className="max-w-2xl"><p className="text-2xl leading-snug lg:text-4xl">{p.overview}</p><div className="mt-16 grid gap-10 border-t border-line pt-8 sm:grid-cols-2"><div><h2 className="font-mono text-xs uppercase tracking-widest text-accent">The problem</h2><p className="mt-4 leading-relaxed text-muted">{p.problem}</p></div><div><h2 className="font-mono text-xs uppercase tracking-widest text-accent">Our direction</h2><p className="mt-4 leading-relaxed text-muted">{p.solution}</p></div></div><div className="mt-16 border-t border-line pt-8"><h2 className="font-mono text-xs uppercase tracking-widest text-accent">Capabilities</h2><div className="mt-5 flex flex-wrap gap-3">{p.capabilities.map((x) => <span key={x} className="border border-line px-4 py-3 text-sm text-muted">{x}</span>)}</div></div><Link href="/contact" className="mt-16 inline-flex items-center gap-3 border-b border-foreground/50 pb-3 hover:border-accent hover:text-accent">Start a conversation <ArrowUpRight className="size-4" /></Link></div></div></article><footer className="border-t border-line px-6 py-8 font-mono text-[10px] uppercase tracking-widest text-muted lg:px-10">{projectName(slug)} / Natural Intellects</footer></main> }
