import Link from 'next/link'
import { ArrowLeft, ArrowUpRight, Mail, MessageCircle } from 'lucide-react'
import { contact } from '@/data/site'
import { SiteNav } from '@/components/site-chrome'

export default function ContactPage() { return <main><SiteNav /><article className="mx-auto min-h-screen max-w-7xl px-6 pb-24 pt-40 lg:px-10 lg:pt-52"><Link href="/#top" className="font-mono text-xs uppercase tracking-widest text-muted hover:text-accent"><ArrowLeft className="mr-2 inline size-3" /> Back to Natural Intellects</Link><div className="mt-20 max-w-4xl"><div className="eyebrow">Start a conversation</div><h1 className="mt-7 text-6xl tracking-[-.07em] lg:text-9xl">What are you<br /><span className="text-accent">trying to build?</span></h1><p className="mt-10 max-w-xl text-xl leading-relaxed text-muted">Tell us what you&apos;re trying to build, improve, or solve. Choose the path that works best for you.</p><div className="mt-16 grid gap-px border border-line bg-line sm:grid-cols-2"><a href={contact.whatsapp} target="_blank" rel="noreferrer" className="group bg-panel p-8 hover:bg-background sm:p-12"><MessageCircle className="size-6 text-accent" /><h2 className="mt-20 text-2xl">Continue on WhatsApp</h2><p className="mt-3 text-sm text-muted">Open a direct conversation with Natural Intellects.</p><ArrowUpRight className="mt-8 size-5 text-muted group-hover:text-accent" /></a><a href={`mailto:${contact.email}`} className="group bg-panel p-8 hover:bg-background sm:p-12"><Mail className="size-6 text-accent" /><h2 className="mt-20 text-2xl">Send an email</h2><p className="mt-3 text-sm text-muted">{contact.email}</p><ArrowUpRight className="mt-8 size-5 text-muted group-hover:text-accent" /></a></div><div className="mt-10 border border-line bg-panel p-8 sm:p-10"><div className="eyebrow">Call directly</div><div className="mt-6 flex flex-col gap-3 sm:flex-row sm:gap-8">{contact.phones.map((phone) => <a key={phone} href={`tel:${phone.replaceAll(' ', '')}`} className="font-mono text-sm tracking-wide text-foreground hover:text-accent">{phone}</a>)}</div></div></div></article></main> }
